# 多语言功能实现说明

## 功能概述

已成功为古汉语智能标注平台添加多语言支持，包括：
- 简体中文（默认）
- 繁体中文
- English（英文）

## 修改的文件

### 1. `client/js/utils.js`
- 添加了完整的翻译字典 `translations`，包含三种语言的所有界面文本
- 添加了 `getCurrentLanguage()` 函数获取当前语言
- 添加了 `setCurrentLanguage(lang)` 函数保存语言选择
- 添加了 `t(key)` 函数获取翻译文本

### 2. `client/index.html`
- 在语言下拉菜单中添加了繁体中文和英文选项

### 3. `client/js/ui-manager.js`
- 添加了 `currentLanguage` 属性
- 实现了 `applyLanguage()` 函数，更新所有界面文本
- 修改了 `selectLanguage(lang)` 函数，实现语言切换
- 更新了所有硬编码文本，使用 `t()` 函数进行翻译
- 更新了所有提示消息、确认对话框、按钮文本等

### 4. `client/js/app.js`
- 更新了登出确认对话框使用翻译

### 5. `client/pages/login.html`
- 添加了语言选择器
- 实现了页面文本的多语言支持
- 添加了 `applyLanguage()` 函数

### 6. `client/pages/register.html`
- 添加了语言选择器
- 实现了页面文本的多语言支持
- 添加了 `applyLanguage()` 函数

## 使用说明

1. **语言切换**：
   - 在主页面右上角，点击当前语言（默认"简体中文"）
   - 在下拉菜单中选择"简体中文"、"繁體中文"或"English"
   - 页面会立即切换到所选语言

2. **语言持久化**：
   - 用户选择的语言会保存在浏览器的 localStorage 中
   - 下次访问时会自动应用上次选择的语言

3. **数据保持不变**：
   - 来自 `data.json` 的数据（如文档内容、项目名称等）保持原样
   - 只有界面文本（按钮、标签、提示信息等）会被翻译

## 翻译覆盖范围

- ✅ 页面标题和标语
- ✅ 导航按钮（保存文档、返回项目、个人信息、登出）
- ✅ 主页面（项目管理、搜索、新建项目等）
- ✅ 文档列表（新建文档、导入文档、导出文档等）
- ✅ 文档编辑器（标签页、工具栏、侧边栏）
- ✅ 实体标注功能
- ✅ 古文解析功能
- ✅ 自动分词功能
- ✅ 模态框（创建项目、创建文档等）
- ✅ 确认对话框
- ✅ 提示消息（成功、错误、警告）
- ✅ 登录页面
- ✅ 注册页面
- ✅ 占位符文本
- ✅ 模型选择对话框

## 技术实现

1. **翻译字典**：使用三层对象结构，第一层是语言，第二层是功能模块，第三层是具体文本
2. **语言存储**：使用 `localStorage.getItem('app_language')` 保存用户选择
3. **实时切换**：通过 `applyLanguage()` 函数遍历所有界面元素并更新文本
4. **最小修改**：只修改了必要的文件，保持了原有功能的完整性

## 注意事项

- 所有界面文本已翻译，但用户创建的数据（项目名、文档内容等）保持原样
- 语言切换是即时的，无需刷新页面
- 翻译覆盖了所有主要功能模块
- 代码经过语法检查，无linter错误

