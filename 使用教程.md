# 使用教程

## 一、环境配置

### 1. Python 环境（用于 古文解析、古文答疑的AI服务和分词服务）

**macOS:**
```bash
# 检查 Python 版本（需要 Python 3.7+）
python3 --version

# 安装 AI 服务依赖
cd server/AI
pip3 install -r requirements.txt

# 安装分词服务依赖
cd ../seg
pip3 install flask flask-cors jieba
```

**Windows:**
```bash
# 检查 Python 版本（需要 Python 3.7+）
python --version

# 安装 AI 服务依赖
cd server\AI
pip install -r requirements.txt

# 安装分词服务依赖
cd ..\seg
pip install flask flask-cors jieba
```

### 2. Node.js 环境（用于用户数据服务）

**macOS:**
```bash
# 检查 Node.js 版本（需要 Node.js 14+）
node --version

# 安装用户服务依赖
cd server/user
npm install
```

**Windows:**
```bash
# 检查 Node.js 版本（需要 Node.js 14+）
node --version

# 安装用户服务依赖
cd server\user
npm install
```

## 二、启动后端服务

### 1. 启动 AI 服务（端口 5004）

**macOS:**
```bash
cd server/AI
python3 ai.py
```

**Windows:**
```bash
cd server\AI
python ai.py
```

### 2. 启动分词服务（端口 5001）

**打开新终端窗口**

**macOS:**
```bash
cd server/seg
python3 seg_server.py
```

**Windows:**
```bash
cd server\seg
python seg_server.py
```

### 3. 启动用户数据服务（端口 5002）

**打开新终端窗口**

**macOS:**
```bash
cd server/user
npm start
```

**Windows:**
```bash
cd server\user
npm start
```

## 三、启动前端

直接双击打开 `client/index.html` 文件，或在浏览器中打开该文件。

## 四、验证服务状态

所有三个服务启动后，终端应显示如下信息：

- **AI 服务**: `服务地址: http://0.0.0.0:5004`
- **分词服务**: `Running on http://0.0.0.0:5001`
- **用户服务**: `📡 端口: 5002`

## 五、常见问题

### 1. 端口被占用
如果提示端口已被占用，请关闭占用该端口的程序，或修改服务代码中的端口号。

### 2. Python 模块未找到
运行 `pip3 install` 或 `pip install` 重新安装所需依赖。

### 3. Node.js 依赖错误
删除 `server/user/node_modules` 文件夹，重新运行 `npm install`。

### 4. 浏览器无法连接服务
确保三个后端服务都已成功启动，检查终端是否有错误信息。

