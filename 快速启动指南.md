# 快速启动指南

## 🎯 项目简介

这是一个**古汉语智能标注和分析平台**，主要包含两个子项目：

1. **iAnctChinese-Client-main** - 主应用（前端+后端服务）
2. **qwen-project-main** - AI解析服务（可选）

---

## ⚡ 5分钟快速启动

### 步骤1：启动用户管理服务（必需）

```bash
cd "iAnctChinese-Client-main/server"
npm install              # 首次运行需要安装依赖
npm start                # 启动服务
```

看到以下提示说明成功：
```
🚀 用户管理服务已启动 (Express)
📡 端口: 5002
```

**保持这个终端运行！**

---

### 步骤2：启动分词服务（必需）

**新开一个终端窗口：**

```bash
cd "iAnctChinese-Client-main"
pip install flask flask-cors jieba    # 首次运行需要安装依赖
python seg_server.py                   # 启动服务
```

看到 `Running on http://0.0.0.0:5001` 说明成功。

**保持这个终端运行！**

---

### 步骤3：打开前端页面

**方式1：直接打开（简单）**
```bash
# 双击打开以下文件
iAnctChinese-Client-main/index.html
```

**方式2：使用本地服务器（推荐）**

**新开一个终端窗口：**
```bash
cd "iAnctChinese-Client-main"
python -m http.server 8000
```

然后在浏览器访问：`http://localhost:8000`

---

### 步骤4：登录/注册

1. 如果是首次使用，点击"注册"创建账号
2. 登录后即可使用项目管理、文档编辑、实体标注、自动分词功能

---

## 🤖 启动AI服务（可选 - 古文解析功能）

如果需要使用**古文解析**功能，需要额外启动AI服务：

### 前置条件
- 需要申请大模型API（OpenAI、通义千问、文心一言等）
- 需要配置API密钥

### 启动步骤

**新开一个终端窗口：**

```bash
# 1. 进入AI服务目录
cd "qwen-project-main/qwen-project-main"

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置API密钥（重要！）
# 用文本编辑器打开 qwen.py
# 在文件顶部找到配置区域，填写您的API密钥：

API_TYPE = "openai"           # 选择API类型
API_KEY = "sk-xxxxx"          # ⚠️ 填写您的API Key
API_BASE_URL = "https://api.openai.com/v1"
MODEL_NAME = "gpt-3.5-turbo"

# 详细配置方法请查看：API配置示例.md

# 4. 启动服务
python qwen.py
```

看到以下提示说明成功：
```
🚀 古文解析服务已启动 (基于大模型API)
📡 端口: 5000
```

### 获取API密钥

**推荐方案（国内用户）：阿里云通义千问**

1. 访问 https://dashscope.aliyun.com/
2. 注册并登录
3. 开通"通义千问"服务
4. 在控制台获取API Key
5. 在 `qwen.py` 中配置：
   ```python
   API_TYPE = "qwen"
   API_KEY = "sk-xxxxx"  # 你的API Key
   MODEL_NAME = "qwen-turbo"
   ```

更多API配置方法请查看：[API配置示例.md](qwen-project-main/qwen-project-main/API配置示例.md)

---

## 📊 服务端口总览

启动后，各服务运行在以下端口：

| 服务 | 端口 | 状态 | 说明 |
|------|------|------|------|
| 前端页面 | 8000 | 可选 | 也可直接打开HTML |
| AI解析 | 5000 | 可选 | 需要配置API Key |
| 分词服务 | 5001 | **必需** | jieba分词 |
| 用户服务 | 5002 | **必需** | 用户管理和数据存储 |

---

## 🎮 功能说明

### 必需服务启动后可用的功能：
- ✅ 用户注册/登录
- ✅ 项目管理（创建/编辑/删除）
- ✅ 文档管理（创建/编辑/删除/导入）
- ✅ 实体标注（人物、地名、时间等）
- ✅ 自动分词（基于jieba）
- ✅ 数据云端同步

### AI服务启动后额外可用：
- ✅ 古文智能解析（字面意思、哲学思想、现实意义）

---

## 🛑 停止服务

在各个终端窗口按 `Ctrl + C` 即可停止对应服务。

---

## 🔧 故障排除

### 问题1：端口被占用

**错误提示：** `Error: listen EADDRINUSE: address already in use :::5002`

**解决方案：**

Mac/Linux:
```bash
lsof -i :5002        # 查看占用端口的进程
kill -9 <PID>        # 终止该进程
```

Windows:
```bash
netstat -ano | findstr :5002    # 查看占用端口的进程
taskkill /PID <PID> /F          # 终止该进程
```

### 问题2：依赖安装失败

**npm安装失败：**
```bash
# 切换到国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

**pip安装失败：**
```bash
# 使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
```

### 问题3：前端打开后无法登录

**检查清单：**
1. ✅ 用户服务（5002）是否正在运行？
2. ✅ 浏览器控制台（F12）是否有错误？
3. ✅ 网络请求是否被拦截？

**解决方案：**
```bash
# 重启用户服务
cd iAnctChinese-Client-main/server
npm start

# 检查服务状态
curl http://localhost:5002/api/health
```

### 问题4：AI解析功能不可用

**检查清单：**
1. ✅ AI服务（5000）是否启动？
2. ✅ API密钥是否正确配置？
3. ✅ 网络连接是否正常？
4. ✅ API账户余额是否充足？

**解决方案：**
```bash
# 检查AI服务状态
curl http://localhost:5000/api/health

# 测试API调用
curl -X POST http://localhost:5000/api/analyze \
  -H "Content-Type: application/json" \
  -d '{"text":"学而时习之"}'
```

### 问题5：分词功能不可用

**检查清单：**
1. ✅ 分词服务（5001）是否启动？
2. ✅ jieba库是否安装？

**解决方案：**
```bash
# 安装jieba
pip install jieba

# 启动分词服务
cd iAnctChinese-Client-main
python seg_server.py
```

---

## 📁 数据存储位置

- **用户数据**：`iAnctChinese-Client-main/data/data.json`
- **本地缓存**：浏览器 localStorage

---

## 💡 使用建议

### 对于学习和测试
```bash
# 只启动必需服务（步骤1-3）
✅ 用户服务（5002）
✅ 分词服务（5001）
✅ 前端页面

# 暂时跳过AI服务（避免API费用）
❌ AI服务（5000）
```

### 对于完整体验
```bash
# 启动所有服务（步骤1-4）
✅ 用户服务（5002）
✅ 分词服务（5001）
✅ 前端页面
✅ AI服务（5000）  # 需要配置API Key
```

---

## 🎯 快速测试

启动所有服务后，可以进行以下测试：

1. **注册账号**：用户名 `test`, 邮箱 `test@qq.com`, 密码 `123456`
2. **创建项目**：名称 `测试项目`, 描述 `这是一个测试`
3. **创建文档**：名称 `论语`, 内容 `学而时习之，不亦说乎`
4. **实体标注**：选中"学"字，标注为"概念"
5. **自动分词**：切换到"自动分词"标签，点击"对当前内容分词"
6. **古文解析**：切换到"古文解析"标签，点击"解析当前内容"（需要AI服务）

---

## 📚 详细文档

- **项目说明**: [iAnctChinese-Client-main/README.md](iAnctChinese-Client-main/README.md)
- **AI配置**: [qwen-project-main/qwen-project-main/API配置示例.md](qwen-project-main/qwen-project-main/API配置示例.md)
- **云端同步**: [iAnctChinese-Client-main/云端同步使用指南.md](iAnctChinese-Client-main/云端同步使用指南.md)
- **更新说明**: [qwen-project-main/qwen-project-main/更新说明.md](qwen-project-main/qwen-project-main/更新说明.md)

---

## 🌟 一键启动脚本（高级用户）

**Mac/Linux** - 创建 `start.sh`:
```bash
#!/bin/bash

# 启动用户服务
cd "iAnctChinese-Client-main/server"
npm start &

# 启动分词服务
cd ../..
cd "iAnctChinese-Client-main"
python seg_server.py &

# 启动前端服务
python -m http.server 8000 &

echo "✅ 所有服务已启动！"
echo "🌐 请访问: http://localhost:8000"
```

使用方法：
```bash
chmod +x start.sh
./start.sh
```

**Windows** - 创建 `start.bat`:
```batch
@echo off
start cmd /k "cd iAnctChinese-Client-main\server && npm start"
start cmd /k "cd iAnctChinese-Client-main && python seg_server.py"
start cmd /k "cd iAnctChinese-Client-main && python -m http.server 8000"
echo All services started!
```

双击运行即可。

---

**祝您使用愉快！** 🎉

如有问题，请查看详细文档或检查各服务的日志输出。

