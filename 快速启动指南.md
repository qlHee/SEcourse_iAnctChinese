# 快速启动指南

## 🚀 5分钟快速开始

### 第一步：安装依赖 (2分钟)

```bash
# 安装古文解析服务依赖
cd qwen-project-main/qwen-project-main
pip install -r requirements.txt

# 安装分词服务依赖
cd ../../iAnctChinese-Client-main
pip install flask flask-cors jieba
```

### 第二步：配置 API Key (2分钟) ⚠️ **必须完成**

#### 2.1 复制配置文件

```bash
cd ../qwen-project-main/qwen-project-main
cp config.example.json config.json
```

#### 2.2 获取 API Key（二选一）

**选项A：阿里通义千问（推荐）**
- 访问：https://dashscope.console.aliyun.com/
- 注册 → 开通服务 → 创建 API Key

**选项B：DeepSeek**
- 访问：https://platform.deepseek.com/
- 注册 → 创建 API Key

#### 2.3 填入 API Key

编辑 `config.json`：

```json
{
  "api_provider": "qwen",
  "api_config": {
    "qwen": {
      "api_key": "👈 在这里填入您的通义千问 API Key",
      ...
    },
    "deepseek": {
      "api_key": "👈 在这里填入您的 DeepSeek API Key",
      ...
    }
  }
}
```

### 第三步：启动服务 (1分钟)

#### 终端 1 - 古文解析服务

```bash
cd qwen-project-main/qwen-project-main
python qwen.py
```

✅ 看到以下输出表示成功：
```
============================================================
古文解析 API 服务启动中...
API 提供商: qwen
使用模型: qwen-turbo
服务地址: http://0.0.0.0:5000
============================================================
```

#### 终端 2 - 分词服务

```bash
cd iAnctChinese-Client-main
python seg_server.py
```

### 第四步：打开前端

用浏览器打开：
```
iAnctChinese-Client-main/index.html
```

或使用本地服务器（推荐）：
```bash
cd iAnctChinese-Client-main
python -m http.server 8000
# 访问 http://localhost:8000
```

### 第五步：测试功能

1. **登录/注册**
2. **创建项目** → 点击"新建项目"
3. **创建文档** → 在项目中点击"新建文档"
4. **测试古文解析**：
   - 切换到"古文解析"标签页
   - 输入：`学而时习之，不亦说乎`
   - 点击"解析"按钮
   - 等待 AI 解析结果

✅ 如果看到解析结果，说明一切正常！

## 📋 快速测试 API

```bash
cd qwen-project-main/qwen-project-main
python test_api.py
```

预期看到：
```
🎉 所有测试通过！API 工作正常。
```

## ❓ 常见问题

### Q1: 启动时提示"配置文件不存在"

```bash
cd qwen-project-main/qwen-project-main
cp config.example.json config.json
# 然后填入 API Key
```

### Q2: API 调用失败

检查：
- ✓ API Key 是否正确
- ✓ 网络是否正常
- ✓ 账户是否有余额

### Q3: 端口被占用

修改端口：
- `qwen.py` 最后一行改为 `app.run(host='0.0.0.0', port=5002)`
- `seg_server.py` 最后一行改为 `app.run(host='0.0.0.0', port=5003)`

## 📚 更多文档

- 📖 [README.md](README.md) - 完整项目说明
- 📖 [API配置指南.md](qwen-project-main/qwen-project-main/API配置指南.md) - 详细配置说明
- 📖 [项目修改说明.md](项目修改说明.md) - 修改详情

## 🔑 API Key 填写位置总结

**文件位置**：`qwen-project-main/qwen-project-main/config.json`

**填写内容**：
```json
{
  "api_provider": "qwen",  // 选择 "qwen" 或 "deepseek"
  "api_config": {
    "qwen": {
      "api_key": "sk-xxxxxxxx"  // 👈 填这里（阿里通义千问）
    },
    "deepseek": {
      "api_key": "sk-xxxxxxxx"  // 👈 填这里（DeepSeek）
    }
  }
}
```

根据 `api_provider` 的值，填写对应的 `api_key`。

## ⚠️ 重要提醒

1. **不要**将包含真实 API Key 的 `config.json` 提交到代码仓库
2. 调用 API 会产生费用，请在平台查看定价
3. 建议设置费用预警和上限

---

**祝使用愉快！** 🎉

